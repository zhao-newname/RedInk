# ===========================================
# 小红书图文生成器 - Docker 环境配置
# ===========================================
# 
# 使用方式: docker-compose --env-file .env.docker up -d
# 或复制此文件到 .env，然后执行 docker-compose up -d
#
# ===========================================

# ===========================================
# API 配置 (必需)
# ===========================================

# Google Gemini API Key（用于文案生成）
# 获取地址: https://ai.google.dev/
GOOGLE_CLOUD_API_KEY=your_google_cloud_api_key_here

# 图片生成 API Key（用于生成图片）
# 配合 config/image_providers.yaml 中的配置使用
IMAGE_API_KEY=your_image_api_key_here

# 文字生成 API Base URL（可选）
# 如果使用自定义的文字生成服务，可以配置此项
# 默认: https://api.bltcy.ai
TEXT_API_BASE_URL=https://api.bltcy.ai

# ===========================================
# Flask 服务配置
# ===========================================

# 调试模式（Docker 生产环境建议 False）
FLASK_DEBUG=False

# 服务监听地址（Docker 中必须是 0.0.0.0）
FLASK_HOST=0.0.0.0

# 服务端口（对应 docker-compose.yml 中的映射端口）
FLASK_PORT=12398

# ===========================================
# CORS 跨域配置
# ===========================================

# 允许的前端地址（多个地址用逗号分隔）
# 注意：在 Docker 中，前端可以通过容器内网络访问，也可以通过外部域名访问
# 示例:
#   http://localhost:12398        - 本机访问
#   http://your-domain.com        - 域名访问
#   http://frontend:3000          - Docker 内部网络访问（如果前端也在 Docker 中）
CORS_ORIGINS=http://localhost:12398

# ===========================================
# 文件存储配置
# ===========================================

# 生成图片的输出目录（Docker 中已挂载为 /app/output）
# 相对于项目根目录：./output
OUTPUT_DIR=/app/output

# ===========================================
# Docker 部署配置
# ===========================================

# 暴露给主机的端口（默认 12398）
DOCKER_PORT=12398

# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=INFO

# ===========================================
# 可选：使用代理（如需要翻墙）
# ===========================================

# HTTP 代理
# HTTP_PROXY=http://proxy.example.com:8080

# HTTPS 代理
# HTTPS_PROXY=https://proxy.example.com:8080

# 不代理的地址
# NO_PROXY=localhost,127.0.0.1,api.openai.com
